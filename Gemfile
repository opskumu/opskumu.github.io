# frozen_string_literal: true

# Site is static HTML with .nojekyll (no Jekyll build on GitHub Pages).
# Legacy Jekyll/Gemfile was removed in commit 7a81a16. This minimal
# manifest exists so GitHub dependency graph / Dependabot can reflect
# current reality (no Ruby gems) instead of stale Jekyll-era alerts.
source "https://rubygems.org"
